@model IEnumerable<Manage_Tasks.Models.UserProfile>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>


<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.FullName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PhoneNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JobTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MainResponsibility)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EducationCretification)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Group.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JobTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MainResponsibility)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EducationCretification)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Group.Name)
            </td>
            <td>
                @*<button onclick="Accept(@item.UserID)">Accept</button>*@
                @Html.ActionLink("Accept", "UserIsAccepted", new { id = item.UserID }) |
                @*<script type="text/javascript">
                    //modify as needed to make it pass in what you need.
                    function Accept(id) {
                        $.ajax({
                            url: "@Url.Action("UserIsAccepted", "AcceptUsers")",
                            data: {id : id },
                            cache: false,
                            contentType: false,
                            processData: false,
                            type: "POST",
                            success: function (data) {
                                //TODO: Add whatever if you want to pass a notification back
                                alert('success');
                            },
                            error: function(error) {
                                //TODO: Add some code here for error handling or notifications
                            }
                        }
                </script>*@
                @Html.ActionLink("Edit", "Edit", new { id = item.UserID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.UserID })
            </td>
        </tr>

    }

</table>
